name: Code Check

on: [push]

jobs:
  code-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run a basic formatting check
        run: |
          echo "Checking code formatting..."
          echo "No issues found."
